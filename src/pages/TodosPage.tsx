import React from 'react';

import Todos from "../components/Todos/Todos";

const TodosPage = () => {
    return (
        <div>
            <Todos/>
        </div>
    );
};

export default TodosPage;